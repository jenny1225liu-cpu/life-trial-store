# 2026-06-14 工作日志

## 完成 PRD V3.0 全部页面搭建

### 新增文件
- `src/main.tsx` - 重写为 React Router HashRouter 入口
- `src/pages/AppLayout.tsx` - 共享布局 + 底部7项导航栏
- `src/pages/HomePage.tsx` - 首页（价值展示、用户旅程、4大功能卡片、平台数据）
- `src/pages/ProfilePage.tsx` - 现实档案馆（4步引导：学业档案→城市偏好→薪资预算→维度权重）
- `src/pages/CareerMapPage.tsx` - 职业地图中心（按生活方式/按行业双视图）
- `src/pages/IndustryPage.tsx` - 行业详情页（趋势、热门技能、代表企业、关联岗位）
- `src/pages/JobPage.tsx` - 岗位详情页（7维评分、薪资表格、技能、风险、关联试玩）
- `src/pages/FavoritesPage.tsx` - 岗位候选库（收藏/删除/对比入口）
- `src/pages/ComparePage.tsx` - 职业对比中心（2-3岗位多维对比+小结）
- `src/pages/ReportPage.tsx` - 职业决策报告（AI生成+画像+推荐+行动建议）
- `supabase/migrations/004_career_database.sql` - 职业数据库schema（10张新表）
- `supabase/migrations/005_seed_career_data.sql` - 种子数据（14行业、17城市、15岗位、薪资、风险、市场分析）

### 修改文件
- `src/App.tsx` - 移除底部iPhone indicator和footer（已迁至AppLayout）
- 安装依赖: react-router-dom

### 构建状态
- TypeScript 检查通过
- Vite build 成功 (790KB JS, 60KB CSS)

---

## 完成数据层接入（Supabase + 本地降级）

### 新增文件
- `src/lib/supabase.ts` - 前端 Supabase 客户端 + 全部类型定义（Industry, Job, City, SalaryData 等）
- `src/lib/db.ts` - 数据访问层，Supabase 优先 + 本地 mock 降级策略
  - `isDbAvailable()` 检测 Supabase 可用性（缓存结果）
  - `getIndustries/getIndustry/getJobs/getJob/getCities/getSalaryByJob/getRisksByJob/getMarketAnalysis` 异步查询
  - `LIFESTYLE_CATEGORIES` 生活方式分类数据
  - `saveProfileLocal/loadProfileLocal` 用户档案 localStorage 读写
  - `saveFavoritesLocal/loadFavoritesLocal/addFavoriteLocal/removeFavoriteLocal/isFavoritedLocal` 收藏管理
- `supabase/migrations/006_full_migration.sql` - 一键迁移脚本（DROP旧表+建表+种子+RLS+索引+触发器）

### 重写页面（接入数据层）
- `CareerMapPage.tsx` - 异步加载行业+岗位，动态计算岗位数
- `IndustryPage.tsx` - 异步加载行业详情+关联岗位，代表企业本地数据
- `JobPage.tsx` - 异步加载岗位+风险+薪资，收藏功能接入 localStorage
- `FavoritesPage.tsx` - 从 localStorage 读取收藏，匹配岗位详情
- `ComparePage.tsx` - 异步加载岗位列表，默认选中收藏项
- `ProfilePage.tsx` - 城市列表从数据层获取，档案保存接入 saveProfileLocal

### Supabase 现状
- industries 表存在但 schema 不匹配（缺少 icon 列）且无数据
- 其他表（jobs, cities 等）尚未创建
- 前端数据层自动降级到本地 mock，应用正常运行
- 需用户在 Supabase SQL Editor 中执行 006_full_migration.sql 才能启用数据库

### 构建状态
- Vite build 成功 (800KB JS, 61KB CSS)
- 所有页面正常渲染

---

## 四线并行推进：后端AI + 页面交互 + 部署配置 + UI打磨

### 后端 AI 服务
- `server.ts` 新增 `POST /api/generate-report` 接口，支持 AI 生成职业决策报告 + fallback
- `api/generate-report.ts` 新建 Vercel Serverless 版本
- `src/ai-provider.ts` 新增 `ReportResult` 类型、`generateReport` 方法、`buildReportPrompt`/`parseReportResponse`
- 后端本地启动验证: AI Provider=gemini, DB=connected, health=ok

### 页面交互完善
- `ReportPage.tsx` 完全重写: 调用 `/api/generate-report` 真实 API，渲染维度分析/推荐职业/行动建议
- `App.tsx` 试玩结算页添加"探索你的职业方向"引导卡片（链接到 /map）
- `src/components/Toast.tsx` 新建全局 toast 通知组件（showToast + AnimatePresence）
- `JobPage.tsx` 收藏/取消收藏接入 toast 反馈
- `AppLayout.tsx` 渲染 `<Toast />`

### 部署配置
- `vercel.json` 更新: 添加 `/api/generate-report` 路由
- SPA 路由规则优化: `/((?!assets/).*)` → `/index.html`

### UI 打磨
- `AppLayout.tsx` 底部导航 7项→5项（试玩/探索/收藏/对比/我的），当前页高亮+小圆点
- `AppLayout.tsx` 移动端适配: safe-area-inset-bottom, overscroll-y-contain, pb-20
- `HomePage.tsx` 新增"立即试玩"入口卡片（脉冲动画红点），平台数据改为异步动态
- `CareerMapPage.tsx` 搜索框加 Search 图标 + 300ms 防抖 + 行业卡片悬停动画
- `ProfilePage.tsx` 步骤指示器连接线动画 + 滑块浮动数值气泡 + 城市选择点击动画

### 构建状态
- Vite build 成功 (806KB JS, 63KB CSS)
- 后端 `npx tsx server.ts` 正常启动，AI + DB 全通
- 待执行: Vercel 部署（需要用户配置 vercel 环境变量）

---

## 全面扩充岗位数据

### 变更
- `src/lib/db.ts` LOCAL_JOBS 从 15 个扩充到 61 个岗位，覆盖全部 14 个行业
  - internet(9): +data_analyst, frontend_engineer, devops_engineer, growth_hacker, project_manager
  - creative(5): +brand_designer, illustrator, copywriter, freelance_designer
  - finance(5): +ib_analyst, quant_trader, risk_analyst, fintech_pm, wealth_advisor
  - education(4): +teacher, curriculum_designer, corporate_trainer, edtech_specialist
  - healthcare(4): +doctor, pharma_researcher, nurse, health_manager
  - ecommerce(5): +crossborder_operator, supply_chain_manager, brand_strategist
  - media(5): +director, video_editor, game_designer
  - hospitality(5): +travel_planner, cultural_planner
  - legal(4): +corporate_counsel, compliance_officer, ip_lawyer
  - food(3): +restaurant_manager, food_rnd, food_creator
  - public(3): +civil_servant, community_worker
  - tea(3): +tea_brand_manager, tea_space_owner
  - automotive(3): +autonomous_drive_eng, battery_engineer, automotive_designer
  - realestate(3): +architect, interior_designer, bim_engineer

- LOCAL_RISKS 从 16 条扩充到 44 条，覆盖所有新岗位
- LOCAL_MARKET 从 14 条扩充到 51 条，覆盖所有新岗位
- LIFESTYLE_CATEGORIES 从 4 类扩充到 6 类（+创意无限、+济世利他）
- `supabase/migrations/007_expand_jobs.sql` - 新增 46 个岗位 + 190 条薪资 + 137 条风险 + 76 条市场分析

### 构建状态
- Vite build 成功
